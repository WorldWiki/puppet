---
:backends: yaml
:yaml:
  :datadir: /etc/puppet/hieradata
:private:
  :datadir: /etc/puppet/private/hieradata
:hierarchy:
  - "private/%{::hostname}"
  - "hosts/%{::hostname}"
  - "private/common"
  - ssl
  - common
